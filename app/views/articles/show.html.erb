<h2 class="text-center">Title: <%= @article.title %></h2>
<div class="well col-xs-8 col-xs-offset-2">
 <h4 class="center"><strong>Description:</strong></h4>
 <hr>
 <%= simple_format(@article.description) %><br>
 <div class="article-meta-details">
    <% if @article.user.username %>
        <small>Created by: <%=link_to @article.user.username, users_admin_path(@article.user) %></small>
    <% else %>
        <small>Created by: <%=link_to @article.user.email, users_admin_path(@article.user) %></small>
    <% end %>
</div>
<br><br><br><br>
<%= link_to "Edit", edit_article_path(@article), class: "btn btn-xs btn-primary" %>
<%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete this article?"}, class: "btn btn-xs btn-danger" %>
<%= link_to 'Back', articles_path, class: "btn btn-xs btn-success" %>